services:
  app:
    build: .

    container_name: pos-api-jcwdol39

    restart: always

    ports:
      - '8001:8000'

    env_file:
      - .env

    volumes:
      - .:/app
      - /app/node_modules
